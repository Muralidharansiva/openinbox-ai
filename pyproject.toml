[project]
name = "openinbox-ai"
version = "0.1.0"
description = "AI email triage environment"
authors = [{name = "MDSon07"}]

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"